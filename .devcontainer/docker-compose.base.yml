version: '3.8'

services:
  node-app-base:
    image: node:20
    restart: always
    working_dir: /app
    environment:
      - DEV_PLATFORM=node
    volumes:
      - ../..:/workspaces:cached
      - ../:/app
